server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /assets/ {
    try_files $uri =404;
    access_log off;
    expires 30d;
  }

  # React SPA fallback
  location / {
    try_files $uri /index.html;
  }
}
